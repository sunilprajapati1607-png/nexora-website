{
  "name": "nexora-website",
  "version": "2.0.0",
  "private": true,
  "description": "nexoraofficial.org — static multi-page site (built from src/ by build.js)",
  "scripts": {
    "build": "node build.js",
    "serve": "node build.js --serve",
    "deploy": "node build.js && npx vercel --prod"
  }
}
